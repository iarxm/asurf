#!/bin/env bash

# Iarom Madden future@iarom.org
# centralise calling of asurf tools

# init

[ -n "$1" ] && { cmd="$1"; shift 1; } || cmd="a"


case $cmd in
    m) msurf $@ ;;
    b) msurf "all" ;;
    w) wsurf $@ ;;
    a) wsurf "a";;
    *) nnn ;;
esac

